name: CLI E2E Tests

on:
  push:
    branches: [main, develop]
    paths:
      - 'lib/**'
      - 'priv/**'
      - 'config/**'
      - 'mix.exs'
      - 'mix.lock'
      - 'cli/**'
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'docker-compose.ci.yml'
      - 'test/cli/**'
      - 'scripts/test-cli.sh'
      - 'docs/cli/**'
      - '.github/workflows/cli-e2e.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'lib/**'
      - 'priv/**'
      - 'config/**'
      - 'cli/**'
      - 'mix.exs'
      - 'mix.lock'
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'docker-compose.ci.yml'
      - 'test/cli/**'
      - 'scripts/test-cli.sh'
      - 'docs/cli/**'
      - '.github/workflows/cli-e2e.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    name: CLI E2E
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout thalamus
        uses: actions/checkout@v4

      - name: Checkout zea-cli
        uses: actions/checkout@v4
        with:
          repository: ZeaCl/zea-cli
          path: zea-cli

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: zea-cli/package-lock.json

      - name: Install zea-cli
        working-directory: zea-cli
        run: |
          npm ci
          npm link
          zea --version

      - name: Install zea-thalamus plugin
        working-directory: cli
        run: npm link

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Thalamus image
        uses: docker/build-push-action@v5
        with:
          context: .
          target: runtime
          load: true
          tags: zea-thalamus:ci
          cache-from: type=gha

      - name: Start Thalamus
        run: |
          echo "Starting Thalamus (CI mode)..."
          docker compose -f docker-compose.ci.yml down -v 2>/dev/null || true
          docker compose -f docker-compose.ci.yml up -d --wait
          echo "Waiting for Thalamus to finish migrations + seeds..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:4100/api/public/health >/dev/null 2>&1; then
              echo "✅ Thalamus ready (${i}s)"
              break
            fi
            sleep 2
          done

      - name: Debug — zea CLI setup
        run: |
          echo "=== zea version ===" && zea --version
          echo "=== zea thalamus --help ===" && zea thalamus --help 2>&1 || true
          echo "=== which zea-thalamus ===" && which zea-thalamus 2>&1 || true
          echo "=== curl health ===" && curl -sv http://localhost:4100/api/public/health 2>&1 || true

      - name: Run CLI E2E tests
        env:
          THALAMUS_API_URL: http://localhost:4100
        run: bash scripts/test-cli.sh

      # ── Teardown ──────────────────────────────────
      - name: Thalamus logs
        if: failure()
        run: docker compose -f docker-compose.ci.yml logs thalamus --tail 50

      - name: Teardown
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v
